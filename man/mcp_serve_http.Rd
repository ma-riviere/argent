% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcp-server.R
\name{mcp_serve_http}
\alias{mcp_serve_http}
\title{Create and serve MCP server from file over HTTP}
\usage{
mcp_serve_http(
  file,
  name = NULL,
  version = NULL,
  groups = NULL,
  host = "127.0.0.1",
  port = 8080,
  ...
)
}
\arguments{
\item{file}{Character. Path to R file with annotated functions. Functions should
use \verb{#' @mcp tool|resource|prompt} annotations. Tools use \verb{#' @param} tags,
resources use \verb{#' @uri} and \verb{#' @mimeType} tags, prompts use \verb{#' @param} tags
for arguments.}

\item{name}{Character. Server name. If NULL, auto-detected from filename.}

\item{version}{Character. Server version. If NULL, defaults to "1.0.0".}

\item{groups}{Character vector. Optional. If provided, only serve tools/resources/prompts
in the specified groups. Use the \verb{@group} annotation to assign functions to groups.}

\item{host}{Character. Host to bind to (default: "127.0.0.1")}

\item{port}{Integer. Port to listen on (default: 8080)}

\item{...}{Additional arguments passed to \code{McpServer$serve_http()}}
}
\value{
NULL (server runs blocking by default)
}
\description{
Parses an R file with annotated functions and serves them as an MCP server
over HTTP. This is a convenience wrapper around creating an McpServer instance,
parsing the file, registering tools/resources/prompts, and starting the server.
}
\examples{
\dontrun{
# Simple usage - auto-detects name from filename
mcp_serve_http("zotero_tools.R", port = 8080)

# Serve only specific groups
mcp_serve_http("all_tools.R", groups = c("zotero", "web"), port = 8080)

# Explicit name and version
mcp_serve_http("tools.R", name = "my-server", version = "2.0.0", port = 8080)

# With custom settings
mcp_serve_http(
    "tools.R",
    name = "my-server",
    port = 8080,
    block = TRUE,
    silent = FALSE
)
}
}
