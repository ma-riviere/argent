% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{as_file_content}
\alias{as_file_content}
\title{Convert inputs to file references for multimodal API requests}
\usage{
as_file_content(..., .provider_options = list())
}
\arguments{
\item{...}{One or more file references. Can be:
\itemize{
\item File IDs from uploaded files (e.g., from \code{upload_file()})
\item File paths (interpretation depends on provider)
}}

\item{.provider_options}{List. Provider-specific options to attach as attributes.
Default is an empty list.}
}
\value{
Character vector with file references. Has attribute \code{argent_input = TRUE}
and each element has \code{argent_input_type = "file_ref"}.
}
\description{
Marks file paths or file IDs as file references to be passed to LLM APIs. This is used
for providers that support file uploads (e.g., Google, Anthropic, OpenAI). The files
should already be uploaded to the provider's file store.
}
