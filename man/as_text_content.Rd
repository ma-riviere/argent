% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{as_text_content}
\alias{as_text_content}
\title{Convert inputs to text content for multimodal API requests}
\usage{
as_text_content(
  ...,
  .vec_len = 999,
  .nchar_max = 999,
  .text_converter = default_text_converter,
  .provider_options = list()
)
}
\arguments{
\item{...}{One or more inputs to convert. Can be:
\itemize{
\item Character strings (used as-is)
\item File paths (local files)
\item URLs (downloaded and processed)
\item R objects (converted to text representation via \code{str()})
}}

\item{.vec_len}{Integer. Maximum vector length to display when converting R objects.
Default is 999.}

\item{.nchar_max}{Integer. Maximum characters per element when converting R objects.
Minimum value is 17. Default is 999.}

\item{.text_converter}{Function. Custom converter function with signature
\verb{function(input, vec_len, nchar_max)}. Default is \code{default_text_converter}.}

\item{.provider_options}{List. Provider-specific options to attach as attributes.
Default is an empty list.}
}
\value{
Character vector with processed text content. Has attribute \code{argent_input = TRUE}
and each element has \code{argent_input_type = "text"}.
}
\description{
Converts various input types (text strings, files, URLs, R objects) into text content
suitable for LLM API requests. Handles automatic detection and conversion of different
file formats including JSON, YAML, CSV, XML, HTML, and plain text.
}
