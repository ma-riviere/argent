% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcp-server.R
\name{mcp_serve_stdio}
\alias{mcp_serve_stdio}
\title{Create and serve MCP server from file over stdio}
\usage{
mcp_serve_stdio(file, name = NULL, version = NULL)
}
\arguments{
\item{file}{Character. Path to R file with annotated functions. Functions should
use \verb{#' @mcp tool|resource|prompt} annotations. Tools use \verb{#' @param} tags,
resources use \verb{#' @uri} and \verb{#' @mimeType} tags, prompts use \verb{#' @param} tags
for arguments.}

\item{name}{Character. Server name. If NULL, auto-detected from filename.}

\item{version}{Character. Server version. If NULL, defaults to "1.0.0".}
}
\value{
NULL (server runs blocking)
}
\description{
Parses an R file with annotated functions and serves them as an MCP server
over stdio (standard input/output). This is a convenience wrapper around
creating an McpServer instance, parsing the file, registering
tools/resources/prompts, and starting the server.
}
\examples{
\dontrun{
# Simple usage - auto-detects name from filename
mcp_serve_stdio("zotero_tools.R")

# Explicit name and version
mcp_serve_stdio("tools.R", name = "my-server", version = "2.0.0")

# Typical usage in an MCP server script
if (!interactive()) {
    mcp_serve_stdio(
        rstudioapi::getActiveDocumentContext()$path,
        name = "my-server"
    )
}
}
}
