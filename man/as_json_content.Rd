% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{as_json_content}
\alias{as_json_content}
\title{Convert inputs to JSON content for multimodal API requests}
\usage{
as_json_content(
  ...,
  .max_rows = 10,
  .json_converter = default_json_converter,
  .provider_options = list()
)
}
\arguments{
\item{...}{One or more inputs to convert. Can be:
\itemize{
\item R objects (converted to JSON via jsonlite)
\item JSON file paths (parsed and re-serialized)
\item JSON URLs (downloaded, parsed, and re-serialized)
}}

\item{.max_rows}{Integer. Maximum number of rows to include when converting data frames.
Default is 10.}

\item{.json_converter}{Function. Custom converter function with signature
\verb{function(input, max_rows)}. Default is \code{default_json_converter}.}

\item{.provider_options}{List. Provider-specific options to attach as attributes.
Default is an empty list.}
}
\value{
Character vector with JSON-formatted content. Has attribute \code{argent_input = TRUE}
and each element has \code{argent_input_type = "text"}.
}
\description{
Converts various input types into JSON-formatted text content suitable for LLM API
requests. Particularly useful for passing structured data (data frames, lists) to models.
}
