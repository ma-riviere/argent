# Apply arguments to an MCP prompt

Apply arguments to an MCP prompt

## Usage

``` r
apply_mcp_prompt(prompt_def, arguments = NULL)
```

## Arguments

- prompt_def:

  List. The prompt definition (e.g. one of the items returned by
  [`mcp_prompts()`](https://ma-riviere.github.io/argent/dev/reference/mcp_integration.md)).

- arguments:

  List. The arguments to pass to the prompt

## Value

The prompt result with messages
